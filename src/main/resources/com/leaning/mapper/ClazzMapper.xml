<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leaning.mapper.ClazzMapper">


    <select id="list" resultType="com.leaning.pojo.Clazz">
        select
        clazz.*,
        emp.name as master_name
        from clazz
        left join emp on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != ''">
                and clazz.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and end != null">
                and clazz.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by clazz.update_time desc
    </select>


</mapper>