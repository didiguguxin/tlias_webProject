<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leaning.mapper.StudentMapper">
        <!-- 批量删除学员 -->
        <delete id="deleteById">
            delete from student where id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </delete>



    <select id="list" resultType="com.leaning.pojo.Student">
        select s.*, c.name as clazz_name
        from student s
        left join clazz c on s.clazz_id = c.id
            <where>
                <if test="name != null and name != ''">
                    and s.name like concat('%',#{name},'%')
                </if>
                <if test="degree != null and degree != ''">
                    AND s.degree = #{degree}
                </if>
                <if test="clazzId != null and clazzId != ''">
                    AND s.clazz_id = #{clazzId}
                </if>
            </where>
        </select>

    <select id="countStudentByClazz" resultType="java.util.Map">
        select clazz.name as clazzName, count(student.id) as num
        from clazz
        left join student on clazz.id = student.clazz_id
        group by clazz.id, clazz.name
    </select>
    <select id="countStudentByDegree" resultType="java.util.Map">
        select
        (case
        when degree=1 then '初中'
        when degree=2 then '高中'
        when degree=3 then '大专'
        when degree=4 then '本科'
        when degree=5 then '硕士'
        when degree=6 then '博士'
        else '其他' end
        ) as name,
        count(*) as value
        from student
        group by degree
    </select>
</mapper>